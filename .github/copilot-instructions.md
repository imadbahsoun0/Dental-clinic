after each backend update, generate frontend API client code using this command `npm run generate:client` in the frontend directory.

backend located at:backend/api
frontend located at:frontend

we should not use 'any' type in TypeScript code. Instead, we should use specific types or interfaces to ensure type safety and maintainability.